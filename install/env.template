# KLH Listing Tool - eBay API credentials.
#
# Paste the values from the master machine's ~/.klh/.env here.
# These are the same across both Kim's and Nicky's PCs because
# they list under the same KLHAutographs seller account.
#
# Do NOT commit this file. It lives at %USERPROFILE%\.klh\.env
# and is outside the git repo.

EBAY_APP_ID=
EBAY_CERT_ID=
EBAY_DEV_ID=
EBAY_SITE=EBAY_GB
EBAY_ENV=production
